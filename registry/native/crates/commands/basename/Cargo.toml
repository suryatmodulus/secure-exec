[package]
name = "cmd-basename"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "basename standalone binary for secure-exec VM"

[[bin]]
name = "basename"
path = "src/main.rs"

[dependencies]
uu_basename = "0.7.0"
