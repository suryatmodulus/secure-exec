[package]
name = "cmd-strings"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "strings standalone binary for secure-exec VM"

[[bin]]
name = "strings"
path = "src/main.rs"

[dependencies]
secureexec-strings-cmd = { path = "../../libs/strings-cmd" }
