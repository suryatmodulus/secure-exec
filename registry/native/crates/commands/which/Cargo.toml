[package]
name = "cmd-which"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "which standalone binary for secure-exec VM"

[[bin]]
name = "which"
path = "src/main.rs"

[dependencies]
shims = { package = "secureexec-shims", path = "../../libs/shims" }
