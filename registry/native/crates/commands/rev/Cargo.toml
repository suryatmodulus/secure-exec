[package]
name = "cmd-rev"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "rev standalone binary for secure-exec VM"

[[bin]]
name = "rev"
path = "src/main.rs"

[dependencies]
secureexec-rev = { path = "../../libs/rev" }
