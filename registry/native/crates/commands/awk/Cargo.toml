[package]
name = "cmd-awk"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "awk standalone binary for secure-exec VM"

[[bin]]
name = "awk"
path = "src/main.rs"

[dependencies]
secureexec-awk = { path = "../../libs/awk" }
