[package]
name = "secureexec-awk"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "awk implementation for secure-exec standalone binaries"

[dependencies]
awk-rs = "0.1.0"
