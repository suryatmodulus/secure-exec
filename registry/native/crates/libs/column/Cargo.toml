[package]
name = "secureexec-column"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "column implementation for secure-exec standalone binaries"

[dependencies]
