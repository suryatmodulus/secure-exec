[package]
name = "secureexec-builtins"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Built-in command implementations (sleep, test/[, whoami) for secure-exec VM"

[dependencies]
libc = "0.2"
wasi-ext = { path = "../../wasi-ext" }
