[package]
name = "secureexec-strings-cmd"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "strings command implementation for secure-exec standalone binaries"

[dependencies]
