[package]
name = "secureexec-find"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "find implementation for secure-exec standalone binaries"

[dependencies]
regex = "1"
