[package]
name = "secureexec-shims"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shim implementations for commands requiring subprocess support"

[lib]
name = "shims"
path = "src/lib.rs"

[dependencies]
wasi-ext = { path = "../../wasi-ext" }
