[package]
name = "secureexec-fd"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "fd-find compatible file finder for secure-exec standalone binaries"

[dependencies]
regex = "1"
