[package]
name = "secureexec-wasi-spawn"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WASI process spawning via host_process FFI with pipe-based stdout/stderr capture"

[dependencies]
wasi-ext = { path = "../../wasi-ext" }

[target.'cfg(target_os = "wasi")'.dependencies]
wasi = "0.11.1"
