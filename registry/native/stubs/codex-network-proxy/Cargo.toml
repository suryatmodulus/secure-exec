[package]
name = "codex-network-proxy"
version = "0.0.0"
edition = "2021"
license = "Apache-2.0"
description = "WASM-compatible stub for codex-network-proxy (no-op network proxy)"

[lib]
name = "codex_network_proxy"
path = "src/lib.rs"
